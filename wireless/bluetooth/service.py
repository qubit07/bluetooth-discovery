class Service:
    def __init__(self, host, protocol, name, provider, port, description):
        self.host = host
        self.protocol = protocol
        self.name = name
        self.provider = provider
        self.port = port
        self.description = description
